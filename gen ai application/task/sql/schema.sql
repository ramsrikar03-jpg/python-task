-- Reference schema for movie_ticket_booking (MySQL Workbench)
-- Your Workbench database should already exist; only run this on a fresh setup.

CREATE DATABASE IF NOT EXISTS movie_ticket_booking;
USE movie_ticket_booking;

-- See your existing tables: movie, shows, booking, ticket, users
